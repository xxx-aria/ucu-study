[project]
name = "ucu-study"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "aria", email = "fuokolps@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
ucu-study = "ucu_study:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
